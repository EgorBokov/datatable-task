import { ContainerLayout } from '@components/layouts/ContainerLayout'

export const PagesListPage  = () => {
    return (
        <ContainerLayout>
            Pages list page
        </ContainerLayout>
    );
};
